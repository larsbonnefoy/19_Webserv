#ifndef MUX_HPP
# define MUX_HPP

# include <iostream>
# include <string>

class Mux
{
	public:
		// Constructors
		Mux();
		Mux(const Mux &copy);
		
		// Destructor
		~Mux();
		
		// Operators
		Mux & operator=(const Mux &assign);
		
	private:
		
};

#endif